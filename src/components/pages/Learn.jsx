const React = require('react');

const Navbar = require('../Navbar');
const Editor = require('../Editor');

const Learn = () => (
  <div>
    <Navbar />
    <Editor />
  </div>
);

module.exports = Learn;
