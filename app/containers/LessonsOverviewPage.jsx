const {connect} = require('react-redux');

const LessonsOverview = require('../components/pages/LessonsOverview');

const mapStateToProps = () => ({
});

const mapDispatchToProps = (dispatch) => ({
});

module.exports = connect(
  mapStateToProps,
  mapDispatchToProps
)(LessonsOverview);
